export const statsSeed = Buffer.from('stats');

export const aiImageGeneratorCounterSeed = Buffer.from(
    'ai_image_generator_counter'
);

export const savedAiImageSeed = Buffer.from('saved_ai_image');

export const metadataSeed = Buffer.from('metadata');

export const masterEditionSeed = Buffer.from('edition');

export const mintAuthoritySeed = Buffer.from('mint');

export const achievementsSeed = Buffer.from('achievements');
